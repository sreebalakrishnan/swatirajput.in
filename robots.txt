# swatirajput.in — Sensing Movement, the work of Swati Rajput.
#
# The site is behind a passcode while the copy is still being written.
# That gate is client-side, so the full HTML is still served to anything that
# asks for it — a crawler would read and index every word behind it. Crawling
# is therefore disallowed until the gate comes off.
#
# TO GO PUBLIC: delete gate.js and its <script> and <style> lines from
# index.html and fr/index.html, then restore the block at the bottom of this
# file in place of the one below.

User-agent: *
Disallow: /

# ----------------------------------------------------------------------------
# The public version, for when the passcode is removed:
#
# User-agent: *
# Allow: /
# Disallow: /system.html
#
# User-agent: GPTBot
# Allow: /
# Disallow: /system.html
#
# User-agent: ClaudeBot
# Allow: /
# Disallow: /system.html
#
# User-agent: PerplexityBot
# Allow: /
# Disallow: /system.html
#
# User-agent: Google-Extended
# Allow: /
# Disallow: /system.html
#
# User-agent: CCBot
# Allow: /
# Disallow: /system.html
#
# Sitemap: https://swatirajput.in/sitemap.xml
# ----------------------------------------------------------------------------
